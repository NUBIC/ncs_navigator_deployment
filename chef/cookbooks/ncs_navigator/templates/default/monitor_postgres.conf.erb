check process postgresql with pidfile <%= @pid %> every 2 cycles
  start program = "/sbin/service postgresql-<%= @version %> start"
  stop program = "/sbin/service postgresql-<%= @version %> stop"
  if failed host localhost port 5432 protocol pgsql then restart
