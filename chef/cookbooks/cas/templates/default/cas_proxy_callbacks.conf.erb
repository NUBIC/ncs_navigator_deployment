check process cas_proxy_callbacks with pidfile <%= @pid %>
  start program = "/sbin/service httpd start"
  stop program = "/sbin/service httpd stop"
  if failed host <%= @host %> port <%= @port %> type tcpssl protocol http
    and request "<%= @callback_path %>" then restart
  if 3 restarts within 3 cycles then stop
  group cas
