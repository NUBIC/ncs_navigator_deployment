<%- paths = @log_file_paths.map { |k, p| %Q{#{k}: "#{p}"} }.join(",\n") %>

exports.config = {
  server: {
    host: "<%= @server_host %>",
    port: <%= @server_port %>
  },
  log_file_paths: { <%= paths %> },
  instance_name: "<%= @instance_name %>" 
}
